CREATE TABLE [dbo].[ACC_Department] (
    [DepartmentId]   INT           NOT NULL,
    [DepartmentName] VARCHAR (200) NULL,
    CONSTRAINT [PK_ACC_Department] PRIMARY KEY CLUSTERED ([DepartmentId] ASC)
);

